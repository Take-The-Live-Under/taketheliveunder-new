{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "bash -c 'python3.13 -m venv venv && source venv/bin/activate && pip install \"setuptools<70.0.0\" wheel && pip install -r requirements.txt'",
    "dev": "npm run setup && bash -c 'source venv/bin/activate && PYTHONPATH=../monitor python main.py'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
